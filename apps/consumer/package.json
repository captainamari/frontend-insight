{
  "name": "@frontend-insight/consumer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. apps/consumer/test"
  },
  "dependencies": {
    "@clickhouse/client": "1.23.1",
    "@frontend-insight/event-contract": "workspace:*",
    "@frontend-insight/server-core": "workspace:*",
    "@frontend-insight/shared-config": "workspace:*",
    "kafkajs": "2.2.4"
  },
  "devDependencies": {
    "tsx": "4.23.1"
  }
}
